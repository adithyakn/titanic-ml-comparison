1. EDA 
    # Load the Dataset, check shape, nulls, dtypes
    # PLot survival rate by gender, class, age
    # Correlation Heatmap

2. Preprocessing
    # Fill the missing Age with Median, drop Cabin
    # Encode Sex and Embarked with LabelEncoder
    # Drop cols (Name, Ticket, PassengerID)
    # Train/test split(80/20)

3. Train all Models
    # Same X_train/X_test for all 6 algorithms

4. Evaluation table
    # Accuracy, precision, Recall, F1 for each model side by side
    # one bar chrt comparing accuracy

5. Best model deep-dive
    # confusion matrix
    # random forest -> Feature importances
    # Decision Trees -> Visualize the Tree
    